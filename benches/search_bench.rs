/*use criterion::{criterion_group, criterion_main, Criterion, black_box};

pub fn search_bench(c: &mut Criterion) {
    c.bench_function("Search bench", |b|  b.iter(||
        {}
    ));
}

criterion_group!(benches, search_bench);

criterion_main!(benches);*/